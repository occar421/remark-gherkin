# remark-gherkin

remark plugin to support Markdown with Gherkin (MDG).

## Development

- Install dependencies:

```bash
vp install
```

- Run the unit tests:

```bash
vp test
```

- Build the library:

```bash
vp pack
```
